import React from 'react';

// The WorldView is primarily handled by the background GameCanvas in App.tsx
// to maintain persistence. This component serves as the Registry entry.
export const WorldView: React.FC = () => {
  return null; 
};